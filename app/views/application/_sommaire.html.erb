<% record = local_assigns[:record] %>
<nav role="navigation" aria-labelledby="fr-summary-title" class="fr-summary">
  <h2 id="fr-summary-title" class="fr-summary__title fr-text--md">Sommaire</h2>
  <ol>
    <% sections.each do |id, label, sous_sections| %>
      <li>
        <a href="#<%= id %>" class="fr-summary__link"><%= label %></a>
        <% if sous_sections %>
          <ol>
            <% sous_sections.each do |sous_id, sous_label| %>
              <li><a href="#<%= sous_id %>" class="fr-summary__link"><%= sous_label %></a></li>
            <% end %>
          </ol>
        <% end %>
      </li>
    <% end %>
  </ol>
  <% if record %>
    <hr class="fr-hr fr-my-2w">
    <p class="fr-text--sm">
      <span class="subtitle">Contenu rédigé par :</span><br>
      <a href="https://www.data.gouv.fr/organizations/direction-interministerielle-du-numerique" rel="noopener external" target="_blank">Direction interministérielle du numérique</a>
      <% if record.cree_le %><br>le <time datetime="<%= record.cree_le.iso8601 %>"><%= l record.cree_le.to_date, format: :long %>.</time><% end %>
      <% if record.modifie_le %><br><span class="fr-text--xs">Modifié le <time datetime="<%= record.modifie_le.iso8601 %>"><%= l record.modifie_le.to_date, format: :long %>.</time></span><% end %>
    </p>
    <a href="#modification-contenu">✒️ Proposer une modification</a>
  <% end %>
</nav>
